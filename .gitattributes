# Auto-generated by the TanStack Router plugin, always written with LF endings.
# Pin it to LF so it doesn't churn against core.autocrlf=true on Windows
# (otherwise it shows as perpetually "modified" and can't be discarded).
src/routeTree.gen.ts eol=lf
